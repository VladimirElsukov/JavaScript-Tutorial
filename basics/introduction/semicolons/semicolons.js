// Необходимость точки с запятой
let x = 10; // Окончание выражения
let y = 20; // Следующее выражение

// Если забыть точку с запятой, возможны ошибки
let z = x + y // Это будет воспринято как одно выражение
console.log(z); // Результат: 30